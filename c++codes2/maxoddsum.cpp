#include<iostream>
using namespace std;

int main()
{
    int x,no_elements;
    cin>>x;
    while(x--)
    {
    
        cin>>no_elements;
        int arr[no_elements],i=0,sum=0,max;
        while(no_elements--)
         {
            cin>>arr[i]; i++;
         }
         
         
    }

    return 0;
}
